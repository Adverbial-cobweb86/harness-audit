**What changed and why:**

**Agents affected:**

- [ ] `bash tests/smoke.sh` passes
- [ ] `python3 tools/build_dist.py` passes
- [ ] Agent references updated with the verification date (if behavior changed)
